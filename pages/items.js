import ItemList from "../components/ItemList";

// Page that displays the itemlist component itself
const ItemsPage = () => {
  return (
    <div>
      <h1>Item List Page</h1>
      <ItemList></ItemList>
    </div>
  );
};

export default ItemsPage;
